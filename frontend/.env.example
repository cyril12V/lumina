# Base des appels API.
# Laisser vide en développement : Vite proxifie /api vers le backend (voir vite.config.ts).
# En production, renseigner l'URL publique du backend, ex. https://api.mon-domaine.fr
VITE_API_URL=
